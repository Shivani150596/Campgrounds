<% include partials/header %>


<div class="container">
    <div class="row">
        <h1 style="text-align:center">EDIT <%= campgrounds.name %></h1>
        <div class="width:30; margin: 25px auto:">
             <form action="/campgrounds/<%= campgrounds._id %>?_method=PUT" method="POST">
                   <div class="form-group">
                        <input class="form-control" type="text" name="campgrounds[name]" value="<%= campgrounds.name %>">
                   </div>
                   <div class="form-group">
                        <input class="form-control" type="text" name="campgrounds[image]"  value="<%= campgrounds.image %>">
                   </div>
                     <div class="form-group">
                        <input class="form-control" type="text" name="campgrounds[description]"  value="<%= campgrounds.image %>">
                   </div>
                   <div class="form-group">
                         <button class="btn btn-lg btn-primary btn-block">SUBMIT</button>
                   </div> 
             </form>
        </div>
    </div>
   
   
</div>

<% include partials/footer %> 